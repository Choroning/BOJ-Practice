#include <iostream>
using namespace std;

int main(void) {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    
    cout << ".  .   ." << endl << "|  | _ | _. _ ._ _  _" << endl << "|/\\|(/.|(_.(_)[ | )(/." << endl;
}