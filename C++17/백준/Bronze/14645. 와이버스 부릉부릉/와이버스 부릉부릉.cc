#include <iostream>
#include <string>
using namespace std;

int main(void) {
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    
    int nStop, pStart, a, b;
    
    cin >> nStop >> pStart;
    for (int i = 0; i < nStop; i++) {
        cin >> a >> b;
    }
    cout << "비와이" << endl;
}